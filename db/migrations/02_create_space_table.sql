CREATE TABLE spaces (id SERIAL PRIMARY KEY, name VARCHAR(40) NOT NULL, description VARCHAR(255) NOT NULL, available_from DATE NOT NULL, available_to DATE NOT NULL, price INT NOT NULL, url VARCHAR, user_id INTEGER REFERENCES users(id));