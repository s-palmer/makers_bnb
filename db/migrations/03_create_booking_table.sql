CREATE TABLE bookings (id SERIAL PRIMARY KEY, start_date DATE NOT NULL, end_date DATE NOT NULL, booking_confirmed BOOLEAN, user_id INTEGER REFERENCES users(id), space_id INTEGER REFERENCES spaces(id), host_id INTEGER REFERENCES users(id));